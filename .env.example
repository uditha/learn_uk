APP_PASSWORD=
SESSION_SECRET=replace-with-a-long-random-string

# Optional on Vercel: Project → Storage → Create Database → KV
# so progress follows you from phone to laptop. Vercel injects:
# KV_REST_API_URL=
# KV_REST_API_TOKEN=
